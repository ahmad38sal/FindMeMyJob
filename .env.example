# Local data root (db, generated resumes, cached job HTML)
DATA_DIR=./data

# Apple internal careers — endpoint + AppleConnect cookie path (filled in once known)
APPLE_INTERNAL_CAREERS_URL=
APPLECONNECT_COOKIE_PATH=
